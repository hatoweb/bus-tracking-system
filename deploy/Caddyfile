{
	auto_https off
}

# HTTPS interno (certificado de Caddy) para GPS.
# La app usa basePath /prototipo_vmt
https://:3443 {
	tls internal
	redir / /prototipo_vmt/ 308
	reverse_proxy bus-tracking-app:3000
}
